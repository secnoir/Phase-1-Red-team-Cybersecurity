Input in a built in python function that takes an input from the user and gives an output according to the user input.

An example of input is shown below:

name = input("Enter your name: ")

print(f"Hello {name} !)

How this code works:

When we run the code we are asked to enter our name like:

Enter your name:

When we enter our name , it prints:

Hello irfan !
